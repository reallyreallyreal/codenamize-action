name: Test
on:
  pull_request:
  push:
    branches:
      - main
      - develop

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: yarn install --frozen-lockfile
    - run: yarn test
